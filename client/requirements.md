## Packages
recharts | Dashboard analytics and charts
date-fns | Date formatting for trips and orders
framer-motion | Smooth animations for page transitions and dialogs

## Notes
Authentication is handled via Replit Auth (/api/login, /api/auth/user)
Use Portuguese (pt-BR) for all UI text
Orders creation requires selecting a trip, a client, and multiple products
